# Shared Agent Instructions

Project name: `skills`.

Use the relevant project skill for task-specific guidance.

- When creating commits, use the `commit-message` skill.
- When creating or updating project-level `README.md` and `README.*.md`, use the `project-readme-writer` skill.
